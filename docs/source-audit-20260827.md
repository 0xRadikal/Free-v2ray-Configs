# بازنگریِ منابع — ۲۷ آگوست ۲۰۲۶

> **موضوع:** چهار منبع کامنت (غیرفعال) شد و دو منبع افزوده شد ⇒ `۲۱ → ۱۹` منبعِ فعال.
>
> **روش:** هیچ تصمیمی بر پایهٔ «تعدادِ کانفیگ»، «HTTP 200» یا شهرتِ مخزن گرفته نشد.
> هر منبع از **همان** آبشارِ واقعیِ خودِ این مخزن گذشت
> (`L0/L1 → L2 → L3×۳` با `xray-knife v10.1.1`) و معیارِ برنده **ارزشِ حاشیه‌ای**
> بود: چند کانفیگِ *پایدار* می‌دهد که هیچ منبعِ دیگری در پیکره ندارد.
>
> این سند همهٔ اعدادِ خام را ثبت می‌کند تا تصمیم‌ها بازبینی‌پذیر باشند، نه ادعا.

---

## ۰. خلاصهٔ اجرایی

| منبع | نقش | کانفیگِ پایدار | ارزشِ حاشیه‌ای | تصمیم |
|:--|:--|--:|--:|:--|
| `mahdibland/sub/sub_merge.txt` | مدافع (HEAVY) | ۸۰ از ۴۰۱۹ (۱.۹۹٪) | ۱۰ | ❌ کامنت |
| `Delta-Kronecker/…/all_configs.txt` | مهاجم (HEAVY) | **۹۵۲** از ۶۲۹۷ (۱۵.۱۲٪) | **۵۲۸** | ✅ افزوده شد |
| `peasoft/NoMoreWalls/list_raw.txt` | مدافع (LIGHT) | ۲ از ۱۵۷ (۱.۲۷٪) | ۲ | ❌ کامنت |
| `iampedii/whitedns-sub/base64.txt` | مهاجم (LIGHT) | **۲۷۵** از ۴۰۵ (۶۷.۹٪) | **۸۱** | ✅ افزوده شد |
| `w1770946466/…/Long_term_subscription_num` | مدافع (LIGHT) | **۰** از ۵۶۰ (۰.۰۰٪) | ۰ | ❌ کامنت |
| `…/PSG_proxy/mix` | مهاجم (LIGHT) | ۱۶ از ۳۱۵ (۵.۰۸٪) | ۱ | ⛔️ افزوده **نشد** |
| `mahdibland/Eternity.txt` | مدافع (LIGHT) | ۷۲ از ۲۰۰ (۳۶.۰٪) | **۰** | ❌ کامنت |

**سودِ خالصِ سنجیده‌شده:** `+۵۱۸` (HEAVY) و `+۷۹` (LIGHT) کانفیگِ پایدارِ یکتا.
**حجمِ خام** هم کاهش نیافت: `۴٬۷۳۶ → ۴٬۹۱۰` خط (`+۱۷۴`) با سه منبعِ کمتر.

---

## ۱. چرا «تعدادِ کانفیگ» معیارِ غلطی است

سه سنجهٔ رایج، هر سه در این مخزن آزموده و رد شدند:

| سنجه | چرا کافی نیست | شاهدِ عددی |
|:--|:--|:--|
| «HTTP 200» | منبعِ مرده هم ۲۰۰ می‌دهد | `w1770946466` → ۲۰۰ ولی **۰** پایدار |
| «تعدادِ کانفیگ» | افزونگی را نمی‌بیند | `Eternity.txt` ۲۰۰ کانفیگ، ولی **۱۰۰٪** زیرمجموعهٔ `sub_merge` |
| «نرخِ پایداری» | تنهایی گمراه است | `Eternity` نرخِ ۳۶٪ دارد ولی ارزشِ حاشیه‌ایش **صفر** است |

پس معیارِ نهایی **SOLE** شد: کانفیگِ پایداری که *هیچ منبعِ دیگری* در کلِ پیکره
ندارد. تنها این سنجه به پرسشِ واقعی جواب می‌دهد: «اگر این منبع را برداریم، کاربر
دقیقاً چه چیزی را از دست می‌دهد؟»

---

## ۲. اعتبارسنجیِ خودِ ابزار — پیش از هر اندازه‌گیری

اگر ابزارِ سنجش کالیبره نباشد، هر عددِ بعدی بی‌معنی است. پس **قبل** از مسابقه،
خودِ آبشار با دو کنترل آزموده شد:

### کنترلِ مثبت — ورودیِ شناخته‌شدهٔ سالم
۶۰ کانفیگ که پروداکشن قبلاً `verified/` تشخیص داده بود:

```
pool=732  sampled=60  L1 kept=60  L2 open=59 (98.33%)
L3 per-run = [45, 45, 43]   ever_ok=47   stable=41
⇒ نرخِ پایداری ۶۹.۴۹٪ از بازها   median=249ms   fast=35  secure=19
```

### کنترلِ منفی — ورودیِ شناخته‌شدهٔ ناسالم
۶۰ کانفیگ از ۸٬۹۳۸ موردی که پروداکشن **رد** کرده بود:

```
pool=8938  sampled=60  L1 kept=60  L2 open=30 (50.0%)
L3 per-run = [0, 2, 1]   stable=0
⇒ نرخِ پایداری ۰.۰۰٪
```

> **نتیجه:** ابزار میانِ «سالم» و «ناسالم» تفکیکِ **۶۹.۵٪ در برابر ۰.۰٪** می‌گذارد.
> پس اعدادِ بخش‌های بعدی سیگنال‌اند، نه نوفه. بی این دو کنترل، تمامِ این سند حدس بود.

نکتهٔ ظریفِ کنترلِ منفی: ۵۰٪ از کانفیگ‌های ردشده در L2 **باز** بودند. یعنی
«پورت باز است» به‌هیچ‌وجه معنیِ «پروکسی کار می‌کند» نیست — و همین دلیلِ وجودیِ
لایهٔ L3 است.

---

## ۳. طرحِ مسابقه — حذفِ رانشِ زمانیِ شبکه

اگر منابع پشتِ‌هم تست شوند، بینِ اولی و آخری ~۲۰ دقیقه فاصله می‌افتد و وضعیتِ
شبکه عوض می‌شود؛ آن‌گاه معلوم نیست تفاوتِ نتیجه از **کیفیتِ منبع** است یا از
**زمانِ تست**. برای بستنِ این راهِ خطا:

> همهٔ خطوطِ بازِ هر ۷ شرکت‌کننده در **یک** مجموعهٔ یکتا ادغام شد و
> **همهٔ آن‌ها در همان ۳ دورِ یکسان** سنجیده شدند.

```
unique_open = 7,623
per_run_ok  = [1482, 1486, 1473]     ← سه دور، پراکندگیِ کم ⇒ شبکه پایدار بود
stable      = 1,325        ever_ok = 1,620      flaky = 18.21%
seconds     = 210.8
```

«پایدار» = موفقیت در **هر سه** دور (`L3_ROUNDS = 3`) — همان تعریفی که
`pipeline.py` برای پر کردنِ `verified/` به‌کار می‌برد.

---

## ۴. نتایجِ خامِ هر شرکت‌کننده

### ۴.۱ لایه‌های L0/L1 و L2

| منبع | خام | یکتا | تکراری | L1 عبور | L2 باز | ٪ باز |
|:--|--:|--:|--:|--:|--:|--:|
| `sub_merge` | ۴٬۰۱۹ | ۳٬۹۲۹ | ۲.۲۴٪ | ۱۰۰.۰٪ | ۱٬۲۸۰ | ۳۱.۸۵٪ |
| **`Delta-Kronecker`** | ۶٬۲۹۷ | ۵٬۸۸۸ | ۶.۵۰٪ | ۹۶.۳٪ | ۵٬۵۷۴ | **۹۱.۹۲٪** |
| `peasoft` | ۱۵۷ | ۱۵۶ | ۰.۶۴٪ | ۹۲.۹۹٪ | ۳۹ | ۲۶.۷۱٪ |
| **`whitedns`** | ۴۰۵ | — | — | ۹۸.۸٪ | ۳۳۲ | ۸۲.۰٪ |
| `w1770946466` | ۵۶۰ | ۵۲۶ | ۶.۰۷٪ | ۹۸.۹۳٪ | ۱۵۵ | ۲۷.۹۸٪ |
| `PSG` | ۳۱۵ | ۳۰۶ | — | ۹۹.۰٪ | ۳۰۴ | ۹۶.۵٪ |
| `Eternity` | ۲۰۰ | ۲۰۰ | ۰.۰۰٪ | ۱۰۰.۰٪ | ۱۳۱ | ۶۵.۵۰٪ |

### ۴.۲ لایهٔ L3 — تستِ واقعیِ پروکسی‌شده

| منبع | ورودیِ L3 | پایدار | ٪ از خام | میانهٔ تأخیر | fast | secure |
|:--|--:|--:|--:|--:|--:|--:|
| `sub_merge` | ۱٬۲۸۰ | ۸۰ | ۱.۹۹٪ | ۳۹۴ms | ۶۲ | ۱۰ |
| **`Delta-Kronecker`** | ۵٬۵۱۰ | **۹۵۲** | **۱۵.۱۲٪** | ۳۶۸ms | ۷۷۱ | ۵۶۸ |
| `peasoft` | ۳۹ | ۲ | ۱.۲۷٪ | ۹۵۱ms | ۱ | ۲ |
| **`whitedns`** | ۳۳۲ | **۲۷۵** | **۶۷.۹۰٪** | **۱۳۷ms** | ۲۶۵ | ۱۴۹ |
| `w1770946466` | ۱۵۵ | **۰** | **۰.۰۰٪** | — | ۰ | ۰ |
| `PSG` | ۳۰۴ | ۱۶ | ۵.۰۸٪ | ۲۴۲ms | ۱۵ | ۶ |
| `Eternity` | ۱۳۱ | ۷۲ | ۳۶.۰۰٪ | ۳۶۰ms | ۶۰ | ۳ |

`whitedns` کم‌نوسان‌ترین هم بود (`flaky = 3.85٪` در برابرِ ۱۳–۲۲٪ بقیه).

---

## ۵. ارزشِ حاشیه‌ای (SOLE) — معیارِ واقعیِ تصمیم

| منبع | کلیدِ پایدار | **تازه** نسبت به پیکره | ٪ تازه | از قبل در `verified/` |
|:--|--:|--:|--:|--:|
| `sub_merge` | ۸۰ | ۴ | ۵.۰۰٪ | ۶۵ |
| **`Delta-Kronecker`** | ۸۷۲ | **۵۲۷** | **۶۰.۴۴٪** | ۲۹۸ |
| `peasoft` | ۲ | ۰ | ۰.۰۰٪ | ۱ |
| **`whitedns`** | ۲۰۶ | **۸۱** | **۳۹.۳۲٪** | ۱۱۳ |
| `w1770946466` | ۰ | ۰ | ۰.۰۰٪ | ۰ |
| `PSG` | ۱۶ | ۱ | ۶.۲۵٪ | ۱۵ |
| `Eternity` | ۷۲ | **۰** | **۰.۰۰٪** | ۶۳ |

### دوئل‌های سربه‌سر

```
sub_merge  vs  Delta-Kronecker : ۸۰ ↔ ۸۷۲  | مشترک ۷۰ | سولِ مدافع ۱۰ | سولِ مهاجم ۵۲۸ ⇒ +۵۱۸
w1770946466 vs PSG             :  ۰ ↔  ۱۶  | مشترک  ۰ | سولِ مدافع  ۰ | سولِ مهاجم   ۱ ⇒   +۱
peasoft    vs  whitedns        :  ۲ ↔ ۲۰۶  | مشترک  ۰ | سولِ مدافع  ۲ | سولِ مهاجم  ۸۱ ⇒  +۷۹
```

**`Eternity` تنها موردی است که ارزشِ حاشیه‌ایش صفرِ مطلق است** — ۷۲ کانفیگِ پایدار
دارد ولی **هیچ‌یک** یکتا نیست. این دقیقاً همان الگویی است که `state.py` برای
شکارش ساخته شده بود، و واقعاً هم شکارش کرد (بخش ۸).

---

## ۶. کالیبراسیونِ جغرافیایی — بستنِ راهِ خطای اصلی

اندازه‌گیری‌ها از خروجیِ **`DE` / colo `FRA`** انجام شد، ولی پروداکشن از
**`US` / colo `ORD`** تست می‌کند. این یک مخدوشگرِ واقعی است و نباید نادیده گرفته
یا حدس زده شود. پس روی ۹ بلابِ **نقطه-در-زمان** (blob در همان SHA‌ای که خطِ لوله
دیده بود، از `GitHub Commits API` با `until=CUTOFF`) کالیبره شد:

| بلاب | خام | نرخِ پروداکشن | نرخِ این محیط | نسبت |
|:--|--:|--:|--:|--:|
| `481726821009` (= `sub_merge`) | ۴٬۰۱۱ | ۲.۱۸۹٪ | ۲.۲۴۰٪ | **۱.۰۲x** |
| `139736418122` (= `peasoft`) | ۲۹۴ | ۰.۳۴۱٪ | ۰.۳۴۱٪ | **۱.۰۰x** |
| `228266726618` (= `w1770946466`) | ۱ | ۰.۰٪ | ۰.۰٪ | — |
| `956223795053` | ۱٬۲۶۹ | ۵.۵۷۵٪ | ۶.۴۳۲٪ | ۱.۱۵x |
| `938997116720` | ۶۳۳ | ۳۲.۱۵٪ | ۲۶.۲۱٪ | ۰.۸۲x |
| `375732985185` | ۱۵۰ | ۵۱.۷۰٪ | ۴۴.۹۰٪ | ۰.۸۷x |
| `693336941099` | ۶۰۳ | ۳۴.۲۰٪ | ۸.۸۷٪ | ۰.۲۶x |
| `491571524123` | ۱۰ | ۵۰.۰٪ | ۲۰.۰٪ | ۰.۴۰x |
| `612871058947` | ۱ | ۰.۰٪ | ۰.۰٪ | — |

```
همبستگیِ رتبه‌ایِ اسپیرمن  ρ = ۰.۹۵   (n = ۹)
```

> **حکم:** ترتیبِ رتبه‌ها تقریباً کامل حفظ می‌شود، و مهم‌تر از آن — هر سه *مدافعِ
> این بازنگری* در نسبتِ **۱.۰۰–۱.۰۲x** می‌افتند. یعنی ضعفشان محصولِ جغرافیای این
> محیط نیست و اعداد قابلِ نقل به پروداکشن‌اند.
> (دو بلابِ با نسبتِ پایین جزوِ مدافعانِ این تصمیم نیستند.)

---

## ۷. دورِ تأییدیِ مستقل

مسابقه در زمانی دیگر و مستقل تکرار شد تا نتیجه یک‌بار-مصرف نباشد:

| منبع | دورِ ۱ | دورِ ۲ | داوری |
|:--|--:|--:|:--|
| `whitedns` | ۲۷۵ | ۲۶۴ | ✅ تکرار شد (۷۹.۵٪) |
| `PSG` | ۱۶ | ۱۷ | ✅ تکرار شد (ضعیف در هر دو) |
| `w1770946466` | ۰ | ۰ | ✅ صفرِ قطعی |
| `peasoft` | ۲ | ۰ | ✅ در حدِّ نوفه |

---

## ۸. شاهدِ مستقل برای هر تصمیم

هیچ منبعی تنها با یک دلیل حذف نشد:

**`mahdibland/Eternity.txt`** — دو شاهدِ مستقل
1. زیرمجموعهٔ محضِ ۱۰۰.۰۰٪ از `sub_merge` (اندازه‌گیریِ ثبت‌شده در `state.py`)
2. `state.json` از پیش با **۸ دورِ پیاپیِ «۰ یکتا»** خودکار غیرفعالش کرده بود
   (کلید `f54d13ad978b`) — یعنی سنجهٔ مستقلِ حافظه هم به همین نتیجه رسیده بود.

**`w1770946466`** — شاهدِ بالادستی، نه فقط تست
آخرین کامیتِ **همین مسیر** در مخزنِ بالادست `2024-03-20T18:13:17Z` است
⇒ ~۲.۴ سال کهنه. تستِ آبشار (۰ از ۵۶۰) و نرخِ پروداکشن (۰.۰۰٪) هم‌خوان‌اند.

**`peasoft`** — هم‌خوانیِ سندباکس و پروداکشن
۱.۲۷٪ در این محیط، ۰.۳۴٪ در پروداکشن، نسبت ۱.۰۰x ⇒ سوگیریِ محیط نیست.

**`Delta-Kronecker`** — انتخابِ فایل از کدِ بالادست
فایلِ اصلی حدس زده نشد؛ از خودِ سورسِ بالادست تعیین شد:
`writer.go:192 → writeFile(cfg.Output.MainFile, all)` و `main.go:106`.

**`PSG`** — انصافِ مسابقه بررسی شد
درختِ مخزن (۴۰ فایلِ xray) بازبینی شد: `mix` تنها فایلِ همه‌پروتکلی است و بقیه
برشِ تک‌پروتکل‌اند. یعنی PSG با بهترین فایلِ خودش شرکت کرد و بازنده شد.

---

## ۹. تحلیلِ ایمنی — چه چیزی می‌توانست بشکند

| ریسک | بررسی | نتیجه |
|:--|:--|:--|
| تستِ قفل‌کنندهٔ شمارش | `test_source_docstring_count_matches_the_actual_list` عددِ «هر N منبع» را از docstring می‌خواند | docstring به «۱۹» به‌روز شد ⇒ ✅ pass |
| تستی که URLِ واقعی را assert کند | جست‌وجوی کاملِ `test_pipeline.py` | هیچ ⇒ ایمن |
| کفِ `MIN_ACTIVE = 8` | فعالِ دورِ بعد = ۶ باقی‌مانده + ۲ جدید = **۸** | ✅ روی کف، زیرِ آن نمی‌رود ⇒ بودجهٔ auto-disable صفر می‌شود (محافظه‌کارانه) |
| زبالهٔ `state.json` | `record_round` کلیدهای بیرونِ لیست را GC می‌کند (D-8) | ۴ ورودیِ یتیم خودکار پاک می‌شوند |
| افتِ حجمِ خروجی | اندازه‌گیریِ زنده | `۴٬۷۳۶ → ۴٬۹۱۰` = **+۱۷۴** |
| رگرسیونِ تست | ۳۹۶/۳۹۷ روی مخزنِ تغییریافته **و** ۳۹۶/۳۹۷ روی مبنای دست‌نخورده، با شکستِ **یکسان** | ✅ رگرسیونِ صفر |

تکشکستِ `test_geo_warm_up_never_double_counts_across_categories` روی مبنای
دست‌نخورده هم بازتولید شد (۳ اجرای پیاپی) ⇒ **از پیش موجود**، نامرتبط با این تغییر.

---

## ۱۰. چرا کامنت، نه حذف

منابعِ کنارگذاشته‌شده **پاک نشدند**، بلکه با دلیلِ عددی‌شان کامنت شدند:

- عددها تنها جایی ثبت شده‌اند که نگهدارندهٔ بعدی همان‌جا می‌بیند که تصمیم گرفته شده.
- جلوی «بازگشتِ چرخه‌ای» را می‌گیرد: کسی شش ماه بعد `peasoft` را دوباره اضافه نکند.
- `all_sources()` فقط عضوهای زندهٔ لیست را برمی‌گرداند، پس کامنت‌ها در شمارش
  نمی‌آیند و هیچ هزینهٔ اجرایی ندارند.

---

## ۱۱. بازتولیدِ اندازه‌گیری‌ها

```
xray-knife : v10.1.1  (SHA-256ِ دوگانه بررسی‌شده: ZIP 39696103eb… / BIN a3b10a40cc…)
Python     : 3.12
L3_ROUNDS  : 3            («پایدار» = موفق در هر سه دور)
FAST_MS    : 800          TOP_N : 100
خروجیِ محیط : DE / colo FRA   (منبع: cp.cloudflare.com/cdn-cgi/trace)
پروداکشن   : US / colo ORD
```

اعدادِ این سند از خطِ لولهٔ خودِ مخزن آمده‌اند (`filters.filter_lines` →
`reachability.check_lines` → `pipeline.run_l3_round` → `pipeline.build_buckets`)،
نه از پیاده‌سازیِ موازی — تا آنچه سنجیده شد همان باشد که در تولید اجرا می‌شود.
